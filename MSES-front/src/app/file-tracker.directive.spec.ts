import { FileTrackerDirective } from './file-tracker.directive';

describe('FileTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new FileTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
